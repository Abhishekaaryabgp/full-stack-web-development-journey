// in arrow implicit we can short more suppose we want to reutn only singlie value we can just implicit
const modulo = (a, b) => a % b; // implicit in just one line it will automatic return
console.log(modulo(55, 10));
