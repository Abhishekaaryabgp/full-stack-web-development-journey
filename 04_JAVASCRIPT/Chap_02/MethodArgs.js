// Search string using indexOf
let str = "King";
console.log(str.indexOf("n"));
// doing multiple thing on 1 string suppose we waant trim plus uppercase both
let str1 = "    New    ";
// console.log(str1.trim().toUpperCase()); // in one output 2 things will be happen ...
let newstr = str1.trim().toUpperCase(); // same above thing will print
console.log(newstr);
