const Details = {
  // we usually define const because of (Details) is just like reference and if we do const it cannot change the address
  name: "Abhi",
  age: 25,
  marks: 70,
  height: 5.6, // in objext the order is not nessasary
};
console.log(Details);
