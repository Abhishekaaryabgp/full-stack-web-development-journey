//  import we can import indivisually
import { sum } from "./index.js";
console.log(sum(2, 55));
import { generate } from "random-words";
console.log(generate()); // this function is importing from the modules
